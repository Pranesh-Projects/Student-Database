spring:
# Database
  datasource:
    username: sql6439702
    password: CS548QR1Sm
    url: jdbc:mysql://sql6.freesqldatabase.com:3306/sql6439702
    driver-class-name: com.mysql.cj.jdbc.Driver
# Hibernate
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
# Server
server:
  error:
    include-message: always
# to see the hibernate related quarries
#logging:
#  level: debug

# logging.level.org.hibernate.SQL = DEBUG